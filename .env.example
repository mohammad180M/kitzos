# Optional: Cloudflare Web Analytics beacon token
# Get it from Cloudflare Dashboard → Analytics → Web Analytics
NEXT_PUBLIC_CF_BEACON_TOKEN=

# On-page ad slots (default off). Set to true only after wiring a provider in AdSlot.
NEXT_PUBLIC_ADS_ENABLED=false
